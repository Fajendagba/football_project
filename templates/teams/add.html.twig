{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Team</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.country) }}
    {{ form_row(form.moneyBalance) }}
    {{ form_end(form) }}
{% endblock %}
